{
  "name": "log-trip",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "react-native-worklets": "^0.6.1"
  }
}
